<UserControl x:Class="GeometricModeling.Views.ProjectiveTransformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeometricModeling.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="400"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <local:Global2DTransformationView Grid.Row="0" VerticalAlignment="Stretch"/>
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" Orientation="Horizontal">
            <ui:Button Command="{Binding CeaseProjectiveCommand}">Вимкнути</ui:Button>
            <ui:Button Command="{Binding ApplyProjectiveCommand}" Margin="10 0 0 0">Ввімкнути</ui:Button>
        </StackPanel>


    </Grid>
</UserControl>
